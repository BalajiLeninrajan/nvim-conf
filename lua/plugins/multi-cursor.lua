return {
  "mg979/vim-visual-multi",
  event = "VeryLazy", -- Loads only when you start typing/moving
  init = function()
    -- Optional: Disable default mappings if you want to define your own
    -- vim.g.VM_default_mappings = 0
  end,
}
